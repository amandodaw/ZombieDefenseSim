# Tasks

- Add `Wander` action definition (preconditions/effects/cost)
- Add fallback selection in planner/replanner when no plan is found
- Implement destination picking (radius sampling + validity checks)
- Wire to existing movement/pathfinding execution
- Add interruption handling (cancel/cleanup on replan)
- Headless validation: `godot --headless --path . --quit`
