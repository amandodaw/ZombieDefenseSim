# Tasks

- Resolve position type consistency (`PositionComponent` vs sprite positions).
- Ensure only one layer owns position updates (prefer physics system).
- Headless validation: `godot --headless --path . --quit`
