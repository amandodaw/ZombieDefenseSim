# Tasks

- Define action failure semantics and implement a minimal pattern.
- Add interruption cleanup rules (if plan is cleared while an action is running).
- Headless validation: `godot --headless --path . --quit`
