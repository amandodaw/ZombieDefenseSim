# Tasks

- Define initial resource model in `CityComponent` (placeholder fields and rules).
- Add removal handling when entities are destroyed.
- Make city lookup explicit and safe.
- Headless validation: `godot --headless --path . --quit`
