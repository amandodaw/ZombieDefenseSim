# Tasks

- Document the ordering requirement: spatial index updates before perception.
- Add optional distance filtering (if desired).
- Ensure new entities randomize their perception timer.
- Headless validation: `godot --headless --path . --quit`
