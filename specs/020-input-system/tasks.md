# Tasks

- Document input actions and expected behavior per mode (`build_mode`, `spawn_human`).
- Add a debug flag or remove `print` from input.
- Ensure one-shot flags are reset by the consuming systems.
- Headless validation: `godot --headless --path . --quit`
