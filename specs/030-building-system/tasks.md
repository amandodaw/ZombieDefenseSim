# Tasks

- Clarify lifecycle: order entity -> building entity (document it and keep consistent).
- Add footprint bounds validation.
- Reduce per-frame allocations in preview.
- Ensure `confirm_order` and `abort_order` are reset appropriately.
- Headless validation: `godot --headless --path . --quit`
