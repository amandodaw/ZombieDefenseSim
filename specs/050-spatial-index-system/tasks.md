# Tasks

- Add a destruction hook or a cleanup pass to avoid stale index entries.
- Add minimal debug tooling (optional) to dump entities in a tile.
- Headless validation: `godot --headless --path . --quit`
