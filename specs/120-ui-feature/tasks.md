# Tasks

- Add a collapsible drawer structure in `scenes/ui.tscn` (header + content).
- Move world signal hookups to `_ready()` and prevent duplicate connections.
- Separate UI logic into section refresh functions (unit control, buildings, resources).
- Improve worker assignment UI layout (labels, spacing, consistent option ids).
- Add buildings section (simple list) and resources section (placeholder text until city resources exist).
- Headless validation: `godot --headless --path . --quit`
