# Tasks

- Add planner safety limits (max nodes expanded / max depth).
- Add optional action cost support.
- Document how goals map to goal-condition dictionaries.
- Headless validation: `godot --headless --path . --quit`
